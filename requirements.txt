# ===========================================================================
# Legislative Intelligence & Market Impact Prediction System
# requirements.txt
#
# Pin major versions; allow patch updates (~=).
# Keep groups ordered by layer (foundation → utilities → future phases).
# ===========================================================================

# ---------------------------------------------------------------------------
# Foundation (required now — Task 0)
# ---------------------------------------------------------------------------
python-dotenv~=1.0          # .env file support for settings.py

# ---------------------------------------------------------------------------
# Testing (required now — Task 0)
# ---------------------------------------------------------------------------
pytest~=8.3
pytest-cov~=5.0             # Code coverage reporting
pytest-asyncio~=0.23        # Async test support (used in scraper tests)

# ---------------------------------------------------------------------------
# Code quality (required now — Task 0)
# ---------------------------------------------------------------------------
flake8~=7.1                 # PEP8 linting
flake8-bugbear~=24.4        # Additional opinionated lint rules
black~=24.8                 # Code formatter
isort~=5.13                 # Import sorter
mypy~=1.11                  # Static type checking

# ---------------------------------------------------------------------------
# Task 1 — Bill Scraping
# ---------------------------------------------------------------------------
httpx~=0.27               # Async HTTP client (uncomment in Task 1)
beautifulsoup4~=4.12      # HTML parsing
# lxml~=5.2                 # Fast XML/HTML parser backend (disabled on Windows to avoid compiling issues)
pdfplumber~=0.11          # PDF text extraction
PyPDF2~=3.0               # PDF metadata extraction

# ---------------------------------------------------------------------------
# Task 2 — Market & Company Data
# ---------------------------------------------------------------------------
# pandas~=2.2               # DataFrame operations
# yfinance~=0.2             # Yahoo Finance market data
# openpyxl~=3.1             # Excel file reading (exchange downloads)
# pyarrow~=16.0             # Parquet read/write

# ---------------------------------------------------------------------------
# Task 3 — Validation
# ---------------------------------------------------------------------------
# pydantic~=2.8             # Schema validation and serialisation

# ---------------------------------------------------------------------------
# Task 4 — NLP Pipeline
# ---------------------------------------------------------------------------
# transformers~=4.43        # Hugging Face transformers (FinBERT, Legal-RoBERTa)
# torch~=2.3                # PyTorch backend
# sentence-transformers~=3.0 # Sentence embeddings
# spacy~=3.7                # NLP pipeline (tokenisation, NER)
# scikit-learn~=1.5         # TF-IDF, dimensionality reduction

# ---------------------------------------------------------------------------
# Task 5 — Mapping
# ---------------------------------------------------------------------------
# rapidfuzz~=3.9            # Fuzzy string matching for company names

# ---------------------------------------------------------------------------
# Task 6 — Label Generation (Event Study)
# ---------------------------------------------------------------------------
# numpy~=1.26               # Numerical operations
# scipy~=1.13               # Statistical tests
# statsmodels~=0.14         # OLS regression (market model)

# ---------------------------------------------------------------------------
# Task 7 — Feature Engineering
# ---------------------------------------------------------------------------
# (covered by pandas, numpy, scikit-learn from above tasks)

# ---------------------------------------------------------------------------
# Task 8 — Model Training
# ---------------------------------------------------------------------------
# lightgbm~=4.4             # Gradient-boosted trees
# optuna~=3.6               # Hyperparameter optimisation
# mlflow~=2.14              # Experiment tracking
# joblib~=1.4               # Model serialisation

# ---------------------------------------------------------------------------
# Task 9 — Prediction API
# ---------------------------------------------------------------------------
# fastapi~=0.111            # REST API framework
# uvicorn[standard]~=0.30   # ASGI server
# shap~=0.45                # SHAP values for explainability

# ---------------------------------------------------------------------------
# Task 10 — Dashboard
# ---------------------------------------------------------------------------
# streamlit~=1.36           # Interactive dashboard (Option A)
# plotly~=5.22              # Interactive charts
